// JSDoc typedef exports to make types uniformly importable

/**
 * @typedef {import('./song').Song} Song
 * @typedef {import('./song').SongWithContributions} SongWithContributions
 * @typedef {import('./song').Contribution} Contribution
 * @typedef {import('./song').ContribRole} ContribRole
 *
 * @typedef {import('./contributor').Contributor} Contributor
 * @typedef {import('./contributor').ContributorWithSongs} ContributorWithSongs
 *
 * @typedef {import('./project').Project} Project
 * @typedef {import('./project').ProjectType} ProjectType
 *
 * @typedef {import('./quotation').QuotationRequest} QuotationRequest
 * @typedef {import('./quotation').QuotationService} QuotationService
 * @typedef {import('./quotation').QuotationStatus} QuotationStatus
 * @typedef {import('./quotation').QuotationFormData} QuotationFormData
 * @typedef {import('./quotation').QuotationOptions} QuotationOptions
 */

export {};
  
